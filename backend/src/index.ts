/**
 * Strapi application lifecycle callbacks.
 */
export default {
  /** Runs before application initialization */
  register() {},

  /** Runs during startup for initial data seeding and background tasks */
  bootstrap() {},
};
